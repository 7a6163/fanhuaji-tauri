{
  "name": "fanhuaji-tauri",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "tauri": "tauri",
    "lint": "biome check",
    "lint:fix": "biome check --fix",
    "check": "tsc --noEmit && biome check"
  },
  "devDependencies": {
    "@biomejs/biome": "2.4.9",
    "@tauri-apps/api": "^2",
    "@tauri-apps/cli": "^2",
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  }
}
